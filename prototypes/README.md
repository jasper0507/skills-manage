# Prototypes (throwaway)

These are **not production code**. They exist only to lock or reject product shape before implementation.

| Path | Status | Use for |
|------|--------|---------|
| `workbench-desktop/` | **Accepted** desk/box baseline (2026-07-14) | Align drag/box/multi-select; do not port as-is. If prototype implies **body-delete**, defer to `CONTEXT.md` (v1 = icon-only recycle). |
| `tag-pick-flow/` | **Rejected** (CLI tags + pick → clipboard) | Negative example only — do **not** implement this path for v1 |

Authoritative product rules live in root `CONTEXT.md` (+ `docs/adr/`). Spec/tickets on GitHub may lag the grilled R2/E2 consensus.
