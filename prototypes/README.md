# Prototypes (throwaway)

These are **not production code**. They exist only to lock or reject product shape before implementation.

| Path | Status | Use for |
|------|--------|---------|
| `workbench-desktop/` | **Accepted** behavior baseline (2026-07-14) | Align v1 分类工作台 interactions; do not port as-is |
| `tag-pick-flow/` | **Rejected** (CLI tags + pick → clipboard) | Negative example only — do **not** implement this path for v1 |

Authoritative product rules live in root `CONTEXT.md`. Spec and tickets: GitHub issues on this repo (`#1` spec, `#2`–`#7` implementation).
